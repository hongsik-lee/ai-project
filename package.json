{
  "name": "ai-board-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.12",
    "sass": "^1.97.3"
  }
}
